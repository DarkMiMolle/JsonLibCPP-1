ToDo:

* operator[string] -> JsonTypes in Json that would allow to use correct Json object if we don't have the C++ correponding class.
* being able to handle array of JsonTypes in Jsonable (the point is that today it can be only an array of only one type)
